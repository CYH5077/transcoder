set(SOURCES

)


set(UPDATED_SOURCES "")
foreach(SOURCE IN LISTS SOURCES)
    list(APPEND UPDATED_SOURCES "${CMAKE_CURRENT_SOURCE_DIR}/${SOURCE}")
endforeach()


target_sources(${TRANSCODER_LIB} PRIVATE ${UPDATED_SOURCES})